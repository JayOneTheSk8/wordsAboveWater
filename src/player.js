class Player {
  constructor(context, name, startX, startY, img) {
    this.context = context;
    this.name = name;
    this.startX = startX;
    this.startY = startY;
    this.img = img;
  }
}

module.exports = Player;
